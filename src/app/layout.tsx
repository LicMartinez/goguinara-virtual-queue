import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fila Virtual Goguinara",
  description: "Sistema de fila virtual para evitar esperas. Únete a la fila desde cualquier lugar y recibe notificaciones cuando sea tu turno.",
  keywords: "fila virtual, cola virtual, sistema de turnos, Goguinara, sin esperas",
  authors: [{ name: "Goguinara" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
